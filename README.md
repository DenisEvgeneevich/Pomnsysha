# Pomnyasha Production Bundle

This repository snapshot contains only the assets intended for production deployment.

- Backend source lives under `backend/`.
- Frontend build-ready source lives under `src/` and `public/`.
- Non-essential documentation and helper scripts were removed to keep the bundle minimal.

To extend or rebuild this bundle, add the necessary tooling and package manifests before running your preferred build pipeline.
